# TODOLIST — بازطراحی داشبورد دانش‌آموزی CS50x انزلی

> نقشه‌ی مرحله‌ای اجرای PRD. هر تسک کوچک، مستقل و قابل‌علامت‌گذاری است.
> **قانون:** همزمان فقط یک فاز `in_progress`. هر فاز با تأیید تم/عملکرد ادامه پیدا می‌کند.
> مرجع کامل: `PRD.md`

---

## نوار پیشرفت کلی
- [!] فاز ۰ — فونداسیون (در حال اجرا)
- [ ] فاز ۱ — داشبورد اصلی
- [ ] فاز ۲ — پیشرفت هفتگی + کوییز
- [ ] فاز ۳ — تکالیف
- [ ] فاز ۴ — رتبه‌بندی + استریک
- [ ] فاز ۵ — منابع
- [ ] فاز ۶ — ابزارهای مطالعه

---

## فاز ۰ — مستندسازی + فونداسیون بصری هاروارد
**هدف:** استخوان هارواردی تمیز که بقیه فازها روی آن ساخته می‌شوند.

### ۰.۱ مستندسازی
- [x] ۰.۱.۱ نوشتن `docs/PRD.md` (سند کامل نیازمندی)
- [ ] ۰.۱.۲ نوشتن `docs/TODOLIST.md` (همین فایل)
- [ ] ۰.۱.۳ به‌روزرسانی `PROJECT_PLAN.md` با ارجاع به فاز‌بندی جدید (لینک، بدون حذف محتوای قبلی)

### ۰.۲ هویت بصری هاروارد
- [ ] ۰.۲.۱ ساخت لوگوی هاروارد `static/img/harvard-shield.svg` (سپر Veritas استایل‌شده، Crimson)
- [ ] ۰.۲.۲ ساخت wordmark `static/img/anzali-wordmark.svg` («Anzali · CS50x» با Crimson)
- [ ] ۰.۲.۳ تعریف favicon از لوگو

### ۰.۳ فونداسیون قالب (`base.html`)
- [ ] ۰.۳.۱ تعریف design tokens هاروارد به‌صورت `<style>` با متغیرهای CSS (`--crimson` و...) در `base.html`
- [ ] ۰.۳.۲ لود فونت Vazirmatn از CDN (با fallback system-ui)
- [ ] ۰.۳.۳ بازطراحی navbar: حداکثر ۵ مورد (داشبورد/تکالیف/منابع/رتبه‌بندی/avatar) با Crimson
- [ ] ۰.۳.۴ نوار افقی تیره زیر navbar (Crimson Dark)
- [ ] ۰.۳.۵ dropdown کاربر (avatar) با: ورود/خروج، مسیر مدرک، XP/استریک
- [ ] ۰.۳.۶ منوی همبرگری برای موبایل (< sm)
- [ ] ۰.۳.۷ استایل flash messages با تم هاروارد (success=سبز، error=قرمز، info=tint)
- [ ] ۰.۳.۸ بازطراحی فوتر (Crimson، متن «ساخته‌شده با Flask · انزلی 🌊»)
- [ ] ۰.۳.۹ تعریف کلاس‌های کمکی Tailwind (`.btn-crimson`, `.card-harvard`, `.badge-crimson`) در `<style>`

### ۰.۴ صفحه‌ی اصلی موقت
- [ ] ۰.۴.۱ بازنویسی `templates/index.html` (جایگزینی «Hi sina») با hero هارواردی + ۴ کارت میان‌بر + معرفی ماژول‌ها (محتوای ایستا، تا فاز ۱ داینامیک شود)

### ۰.۵ بررسی و تأیید
- [ ] ۰.۵.۱ اجرای `flask-app/run.py` و بررسی ظاهر در مرورگر
- [ ] ۰.۵.۲ تست واکنش‌گرایی در موبایل (عرض ۳۶۰px)
- [ ] ۰.۵.۳ بررسی کنتراست و RTL
- [ ] ۰.۵.۴ **نقطه‌ی بازبینی با کاربر** — تأیید تم قبل از ادامه

---

## فاز ۱ — داشبورد اصلی (هسته‌ی پروژه)
**هدف:** جایگزینی صفحه‌ی اصلی موقت با داشبورد داینامیک + پیشرفت مدرک.

### ۱.۱ مدل‌ها و مهاجرت
- [ ] ۱.۱.۱ اضافه‌کردن `WeekProgress` به `models.py` (+ UniqueConstraint)
- [ ] ۱.۱.۲ اضافه‌کردن `Achievement` به `models.py`
- [ ] ۱.۱.۳ به‌روزرسانی `init_db.py` برای ساخت جداول جدید + seed نمونه
- [ ] ۱.۱.۴ اعتبارسنجی مهاجرت روی DB تمیز

### ۱.۲ سرویس‌ها (منطق خالص)
- [ ] ۱.۲.۱ ساخت `services/progress.py`: `get_user_stats(user_id)`, `get_certificate_progress(user_id)`, `get_upcoming_assignments(user_id, limit)`
- [ ] ۱.۲.۲ افزودن `services/streak.py`: محاسبه‌ی استریک روزانه از XPLog
- [ ] ۱.۲.۳ smoke test برای این سرویس‌ها در `tests/`

### ۱.۳ روت و تمپلیت داشبورد
- [ ] ۱.۳.۱ بازنویسی `routes/main.py::index` برای پاس‌دادن داده‌ی داشبورد
- [ ] ۱.۳.۲ بازنویسی `templates/index.html`:
  - [ ] ۱.۳.۲.۱ بنر پیشرفت مدرک (نوار درصد + متن)
  - [ ] ۱.۳.۲.۲ ۴ کارت آمار (XP/استریک/تکالیف فعال/رتبه)
  - [ ] ۱.۳.۲.۳ لیست تکالیف نزدیک‌به‌مهلت (۳تا)
  - [ ] ۱.۳.۲.۴ کارت هفته‌ی جاری
  - [ ] ۱.۳.۲.۵ گرید میان‌برهای ابزار مطالعه (placeholder تا فاز ۶)
- [ ] ۱.۳.۳ ساخت macroهای Jinja در `templates/_partials/` (کارت آمار، نوار پیشرفت) برای استفاده‌ی مجدد

### ۱.۴ صفحه‌ی مسیر مدرک ★
- [ ] ۱.۴.۱ روت `/dashboard/certificate`
- [ ] ۱.۴.۲ تمپلیت: نوار پیشرفت کلی + چک‌لیست الزامات + دستاوردها
- [ ] ۱.۴.۳ لینک از avatar dropdown و بنر داشبورد

### ۱.۵ بررسی
- [ ] ۱.۵.۱ تست با کاربر لاگین‌شده و بدون لاگین
- [ ] ۱.۵.۲ **نقطه‌ی بازبینی با کاربر**

---

## فاز ۲ — پیشرفت هفتگی + کوییز تعاملی
**هدف:** مرور تقلب‌نامه + کوییز با ثبت در DB.

### ۲.۱ داده‌ی محتوای هفته‌ها
- [ ] ۲.۱.۱ تبدیل `src/data/weeks-data.ts` → ماژول پایتون `app/content/weeks.py` (مفاهیم، کد، سؤال‌ها)
- [ ] ۲.۱.۲ اعتبارسنجی ساختار داده با یک تست

### ۲.۲ مدل کوییز
- [ ] ۲.۲.۱ اضافه‌کردن `QuizAttempt` به `models.py`
- [ ] ۲.۲.۲ به‌روزرسانی `init_db.py`

### ۲.۳ سرویس و روت کوییز (HTMX)
- [ ] ۲.۳.۱ `services/quiz.py`: ثبت attempt، بهترین نمره، تحلیل پاسخ‌های غلط
- [ ] ۲.۳.۲ روت POST `/kb/week/<n>/quiz/submit` که fragment برمی‌گرداند (HTMX)
- [ ] ۲.۳.۳ رویداد XP برای کوییز کامل

### ۲.۴ تمپلیت پایگاه دانش
- [ ] ۲.۴.۱ بازطراحی `templates/kb/week.html` با تب‌ها (مفاهیم/کد/کوییز/یادداشت)
- [ ] ۲.۴.۲ کارت‌های مفاهیم (cheat-sheet) با badge اهمیت
- [ ] ۲.۴.۳ کامپوننت کوییز HTMX (progress dots + نتیجه)
- [ ] ۲.۴.۴ نقشه‌ی راه ۹ هفته (macro قابل‌استفاده در داشبورد هم)

### ۲.۵ بررسی
- [ ] ۲.۵.۱ تست تعامل کوییز بدون رفرش کامل
- [ ] ۲.۵.۲ **نقطه‌ی بازبینی**

---

## فاز ۳ — تکالیف (بهبود UX)
**هدف:** تجربه‌ی مدرک‌محور تحویل تکالیف.

- [ ] ۳.۱ سرویس `get_user_submission_status(user_id, assignment_id)` در `submissions.py`
- [ ] ۳.۲ بازطراحی `templates/assignments/index.html`:
  - [ ] ۳.۲.۱ کارت تکلیف مدرک‌محور با countdown مهلت
  - [ ] ۳.۲.۲ وضعیت تحویل کاربر (نشانه «تحویل‌شده»)
  - [ ] ۳.۲.۳ نمایش grade/feedback اگر تصحیح شده
- [ ] ۳.۳ اعتبارسنجی لینک گیت‌هاب بهبودیافته (پیام خطای واضح‌تر)
- [ ] ۳.۴ **نقطه‌ی بازبینی**

---

## فاز ۴ — رتبه‌بندی + XP + استریک (تم هاروارد)
**هدف:** انگیزه‌ی رقابتی با هویت هاروارد.

- [ ] ۴.۱ سرویس `get_user_rank(user_id)` در `xp.py`
- [ ] ۴.۲ بازطراحی `templates/gamification/leaderboard.html`:
  - [ ] ۴.۲.۱ جدول Crimson با مدال
  - [ ] ۴.۲.۲ رتبه‌ی کاربر فعلی هایلایت
  - [ ] ۴.۲.۳ کارت جمع‌بندی شما (XP/استریک/رتبه)
- [ ] ۴.۳ اعمال `services/streak.py` (از فاز ۱) روی نمایش استریک
- [ ] ۴.۴ **نقطه‌ی بازبینی**

---

## فاز ۵ — منابع هفته (بهبود UI)
**هدف:** کارت‌های هارواردی.

- [ ] ۵.۱ بازطراحی `templates/resources/index.html` (کارت‌های Crimson)
- [ ] ۵.۲ بازطراحی `templates/resources/week.html` (لیست فایل تمیز)
- [ ] ۵.۳ (اختیاری) فیلتر دسته‌بندی فایل (slides/notes/pset/src)
- [ ] ۵.۴ **نقطه‌ی بازبینی**

---

## فاز ۶ — ابزارهای مطالعه (منتقل‌شده از Next.js)
**هدف:** بهترین قابلیت‌ها، در بافت درست.

### ۶.۱ فلش‌کارت
- [ ] ۶.۱.۱ مدل `Flashcard` (با الگوریتم Leitner box)
- [ ] ۶.۱.۲ سرویس + روت‌های HTMX (چرخش، علامت‌گذاری)
- [ ] ۶.۱.۳ تمپلیت `templates/study/flashcards.html`

### ۶.۲ تحلیل نقاط ضعف
- [ ] ۶.۲.۱ سرویس بر اساس `QuizAttempt.answers_json`
- [ ] ۶.۲.۲ کارت در داشبورد اصلی

### ۶.۳ چالش زمان‌دار
- [ ] ۶.۳.۱ روت کوییز تصادفی زمان‌دار
- [ ] ۶.۳.۲ تمپلیت با تایمر client-side

### ۶.۴ دستاوردها + تایمر مطالعه
- [ ] ۶.۴.۱ فعال‌سازی سیستم achievement (مدل از فاز ۱)
- [ ] ۶.۴.۲ تایمر مطالعه سبک client-side در داشبورد

### ۶.۵ بررسی نهایی
- [ ] ۶.۵.۱ پاس کردن همه‌ی معیارهای پذیرش PRD بخش ۱۰
- [ ] ۶.۵.۲ **نقطه‌ی بازبینی نهایی**

---

## یادداشت‌های اجرایی
- **اصل طلایی:** سادگی برای آموزش. بین «هوشمند» و «خوانا»، خوانا برنده است.
- **بدون build-step:** Tailwind از CDN، منطق در `services/`، کامنت فارسی.
- **مخرب‌نبودن:** کد Next.js دست‌نخورده می‌ماند.
- **بعد از هر فاز:** اجرای localhost + اسکرین‌شات برای تأیید بصری.

---

*پس از تکمیل هر تسک، `[ ]` را به `[x]` تبدیل کنید.*
